# Core deep learning — install torch separately before running pip install -r:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128

# Model loading
safetensors>=0.4.0
transformers>=4.40.0
accelerate>=0.28.0
sentencepiece>=0.1.99
tokenizers>=0.19.0

# DiT / pipeline internals
einops>=0.7.0

# Video / image I/O
imageio>=2.34.0
imageio-ffmpeg>=0.4.9
Pillow>=10.0.0
opencv-python-headless>=4.8.0

# Data utilities
pandas>=2.0.0
numpy>=1.24.0
tqdm>=4.66.0
ftfy>=6.1.0
regex>=2023.0.0
